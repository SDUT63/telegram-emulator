# Чат-бот для MAX — нужен всегда
maxapi>=1.2.2

# PostgreSQL для production-хранилища (SQLite остаётся для локального пилота)
psycopg[binary]>=3.2,<4

# Webhook HTTP server
flask
aiohttp

# Выгрузка обращений и напоминаний о звонках в Excel
openpyxl

# Асинхронные integration/unit tests
pytest-asyncio>=0.24,<1

# Сборка и проверка бумажных форм согласия. Без неё тест сверки текста
# согласия с подписываемым документом молча пропускается.
python-docx
