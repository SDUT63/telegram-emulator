# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Build directory
build/
dist/

# Database files
data/
*.db
*.sqlite
*.sqlite3

# Log files
logs/
*.log

# Environment files
.env
.env.local
.env.*.local

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# Node.js (for future frontend development)
node_modules/
npm-debug.log*
yarn-error.log*

# Temporary files
tmp/
temp/

# Configuration files with sensitive data
configs/local.yaml
configs/production.yaml

# examples
__pycache__/
*.pyc
examples/__pycache__/
bot_offset_*.txt

# Чат-бот СДУТ: ключ доступа и персональные данные обратившихся.
token.txt
survey_responses.json
survey_responses.json.tmp
survey_responses.json.broken
survey_responses.csv
demo_responses.json
demo_responses.csv

# Сертификаты и секрет вебхука — локальная инфраструктура, не репозиторий.
certs.pem
.webhook_secret

# CRM оператора: пароли, заметки о людях, очередь сообщений.
operators.json
crm_data.json
.crm_secret
outbox/

# Выгрузки в Excel: там имена, телефоны и состояние людей
*.xlsx
!docs/*.xlsx

# Шрифт для сборки мануала: кэш, качается заново за секунду
webapp/инструкция/_шрифт.css

# Модель: счётчик расходов и кэш ответов — рабочие файлы, не исходники
ai_budget.json
ai_cache.json

# Ключи от облака
.env
ai_keys.txt

# Окружение Python, которое создаёт launcher.py
.venv/
venv/
